using System.Collections.Generic;
using Sirenix.OdinInspector;
using UnityEditor;
using UnityEngine;

public class PrefabCreate : MonoBehaviour
{
    // Start is called before the first frame update
    //[Button("CHANGE OBJECT TO PREFAB")]
    //public void ChangeObjectToPrefab(int startIndex,GameObject prefab, Transform holder, List<GameObject> changeObjects)
    //{
    //    foreach (var obj in changeObjects)
    //    {
                
    //        var feed = (GameObject)PrefabUtility.InstantiatePrefab(prefab, this.transform);
    //        feed.transform.parent = holder;
    //        feed.transform.localPosition = obj.transform.localPosition;
    //        feed.transform.localRotation = obj.transform.localRotation;
    //        feed.transform.localScale = obj.transform.localScale;
    //        feed.name = feed.name +" ("+startIndex+")";
    //        startIndex++;
    //    }
    //} 
}
